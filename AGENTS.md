# Agent Instructions

Before meaningful project work, read:

- `/context/README.md`
- `/context/PROJECT_OVERVIEW.md`
- `/context/CURRENT_STATE.md`

Then read only the additional context files relevant to the task. Keep changes surgical, preserve the existing Supabase schema and RLS unless a migration is explicitly approved, and validate with `npm run validate` before publishing.
