import { PointStackFeedView } from "@/components/pointstack/feed/feed-view";

export const metadata = {
  title: "PointStack — BASidekick",
  description: "Connect with BAS professionals, share projects, find work, and grow your network.",
};

export default function PointStackPage() {
  return <PointStackFeedView />;
}
